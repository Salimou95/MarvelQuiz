language: node_js
node_js:
  - 18
  - 20

cache:
  directories:
    - node_modules

install:
  - npm ci

script:
  - npm run build
  - npm test -- --coverage --watchAll=false

branches:
  only:
    - main
    - test

notifications:
  email:
    on_success: change
    on_failure: always

